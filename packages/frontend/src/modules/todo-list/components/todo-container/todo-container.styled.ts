import styled from 'styled-components';
import { SPACES } from '../../../theme';

export const TodoCardList = styled.ul`
  padding-top: ${SPACES.s};
  padding-bottom: ${SPACES.m};
`;

export const Trigger = styled.div`
  height: ${SPACES.s};
`;
